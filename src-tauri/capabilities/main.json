{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal core capability for the tracker shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
